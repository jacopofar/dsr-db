# this __name__ == '__main__' is used to run the script only when
# it is run directly, not when it is imported as a module
# in this case it's irrelevant, but it's a good practice to use it on the main
# script of a module
if __name__ == '__main__':
    print('Hello, World!')